import { Controller } from "@hotwired/stimulus"

// Empty controller for the toast container element.
// Turbo Streams append toasts into this container by targeting its ID.
export default class extends Controller {
}
