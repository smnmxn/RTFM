<%# locals: (error:) %>
<div id="answer-container">
  <div class="bg-error-soft border border-error rounded-xl p-6">
    <div class="flex items-start gap-3">
      <%= heroicon "exclamation-circle", options: { class: "w-6 h-6 text-error flex-shrink-0 mt-0.5" } %>
      <div>
        <p class="text-error-strong font-medium"><%= error %></p>
        <p class="text-error text-sm mt-2">
          Try browsing our
          <%= link_to "help articles", help_centre_path, class: "underline hover:no-underline" %>
          instead.
        </p>
      </div>
    </div>
  </div>
</div>
