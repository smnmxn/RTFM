<%# Standard header for onboarding steps %>
<%# Parameters: title:, subtitle: (optional) %>
<div class="px-6 py-5 border-b app-border">
  <h2 class="text-xl font-semibold app-text"><%= title %></h2>
  <% if local_assigns[:subtitle] %>
    <p class="mt-1 text-sm app-text-secondary"><%= subtitle %></p>
  <% end %>
</div>
