<%# Display completed questions with their answers %>
<%# Parameters: questions - Array of {question:, answer:, skipped:} hashes %>

<div class="app-bg rounded-lg overflow-hidden">
  <div class="max-h-80 overflow-y-auto divide-y divide-app-border">
    <% questions.each do |q| %>
      <div class="px-4 py-3 flex items-start gap-3">
        <% if q[:skipped] %>
          <div class="flex-shrink-0 mt-0.5">
            <%= heroicon "minus", variant: :mini, options: { class: "h-4 w-4 app-text-muted" } %>
          </div>
          <div class="flex-1 min-w-0">
            <p class="text-sm app-text-muted"><%= q[:question] %></p>
            <p class="text-sm app-text-muted italic">Skipped</p>
          </div>
        <% else %>
          <div class="flex-shrink-0 mt-0.5">
            <%= heroicon "check", variant: :mini, options: { class: "h-4 w-4 text-green-500" } %>
          </div>
          <div class="flex-1 min-w-0">
            <p class="text-sm app-text-secondary"><%= q[:question] %></p>
            <p class="text-sm app-text font-medium"><%= q[:answer] %></p>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
