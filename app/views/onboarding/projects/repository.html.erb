<div class="bg-white shadow-sm rounded-xl border border-slate-200">
  <div class="px-6 py-5 border-b border-slate-100">
    <h2 class="text-xl font-semibold text-slate-900">Choose your repository</h2>
    <p class="mt-1 text-sm text-slate-500">
      We'll analyze the code and suggest what to document.
    </p>
  </div>

  <%= turbo_frame_tag "repository_list",
      src: repositories_projects_path(onboarding_project_id: @project.id),
      loading: :lazy do %>
    <div class="p-8 text-center">
      <div class="h-8 w-8 rounded-full border-2 border-slate-200 border-t-violet-600 animate-spin mx-auto"></div>
      <p class="mt-3 text-sm text-slate-500">Loading your repositories...</p>
    </div>
  <% end %>
</div>
