<!DOCTYPE html>
<html>
  <head>
    <title>Setup - supportpages.io</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="supportpages.io">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%# Removed turbo_refreshes_with morph - it can cause CSS to not load on page refresh %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-slate-50 flex flex-col">
    <div data-controller="presence"></div>
    <% if @project&.persisted? %>
      <%= turbo_stream_from @project, :notifications %>
    <% end %>
    <%= render "shared/toasts" %>

    <!-- Header -->
    <header class="bg-white border-b border-slate-200">
      <div class="max-w-4xl mx-auto px-4 py-4 sm:px-6 lg:px-8 flex justify-between items-center">
        <%= link_to projects_path, class: "hover:opacity-80 transition-opacity" do %>
          <%= image_tag "logo.png", class: "h-12", alt: "supportpages.io" %>
        <% end %>
        <% if current_user.projects.where(onboarding_step: nil).any? %>
          <%= link_to "Back to Projects", projects_path, class: "text-sm text-slate-500 hover:text-slate-700" %>
        <% elsif @project&.persisted? %>
          <%= button_to "Cancel", project_path(@project), method: :delete, class: "text-sm text-slate-500 hover:text-slate-700", data: { turbo_confirm: "Cancel setup? This project will be removed." } %>
        <% end %>
      </div>
    </header>

    <!-- Progress Indicator -->
    <% current_step = @project&.onboarding_step_number.to_i %>
    <% current_step = 1 if current_step == 0 && controller_name == "projects" && controller_path == "onboarding/projects" %>
    <% if current_step.positive? %>
      <div class="max-w-4xl mx-auto px-4 py-8 sm:px-6 lg:px-8">
        <%= render "onboarding/shared/progress", current_step: current_step %>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="max-w-4xl mx-auto px-4 pb-12 sm:px-6 lg:px-8 flex-1">
      <% if flash[:notice] %>
<div class="mb-6 flex items-center gap-3 bg-emerald-50 border border-emerald-200 text-emerald-800 px-4 py-3 rounded-lg">
          <%= heroicon "check-circle", variant: :solid, options: { class: "h-5 w-5 text-emerald-500 flex-shrink-0" } %>
          <span><%= flash[:notice] %></span>
        </div>
      <% end %>
      <% if flash[:alert] %>
<div class="mb-6 flex items-center gap-3 bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-lg">
          <%= heroicon "x-circle", variant: :solid, options: { class: "h-5 w-5 text-red-500 flex-shrink-0" } %>
          <span><%= flash[:alert] %></span>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <footer class="bg-white border-t border-slate-200 mt-auto">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-4 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2 text-xs text-slate-400">
        <span>&copy; <%= Date.current.year %> Surface Layer Limited (Company No. 17026012) | 128 City Road, London, EC1V 2NX</span>
        <nav class="flex gap-4">
          <a href="/legal/privacy" class="hover:text-slate-600 transition-colors">Privacy</a>
          <a href="/legal/terms" class="hover:text-slate-600 transition-colors">Terms</a>
        </nav>
      </div>
    </footer>
  </body>
</html>
