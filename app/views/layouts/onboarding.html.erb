<!DOCTYPE html>
<html>
  <head>
    <title>Setup - RTFM</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="RTFM">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-gray-50">
    <!-- Minimal Header -->
    <header class="bg-white shadow-sm">
      <div class="max-w-4xl mx-auto px-4 py-4 sm:px-6 lg:px-8">
        <h1 class="text-2xl font-bold text-gray-900">RTFM</h1>
      </div>
    </header>

    <!-- Progress Indicator -->
    <% if @project&.onboarding_step_number&.positive? %>
      <div class="max-w-4xl mx-auto px-4 py-6 sm:px-6 lg:px-8">
        <%= render "onboarding/shared/progress", current_step: @project.onboarding_step_number %>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="max-w-4xl mx-auto px-4 pb-8 sm:px-6 lg:px-8">
      <% if flash[:notice] %>
        <div class="mb-6 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded">
          <%= flash[:notice] %>
        </div>
      <% end %>
      <% if flash[:alert] %>
        <div class="mb-6 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
